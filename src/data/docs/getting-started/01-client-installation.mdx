---
title: 'Client Installation'
slug: 'client-installation'
section: getting-started
---

import Code from '@/components/code.astro';
import CodeTabs from '@/components/code-tabs/code-tabs.astro';
import CodeTabTrigger from '@/components/code-tabs/code-tab-trigger.astro';
import CodeTabPanel from '@/components/code-tabs/code-tab-panel.astro';

The `haloy` CLI tool runs on your local machine and triggers deployments to your servers.

## Installation

### Shell Script

To install the latest version via the official shell script:

<Code code="curl -fsSL https://sh.haloy.dev/install-haloy.sh | sh" lang="bash" />

### Homebrew

If you are using macOS or Linux with Homebrew:

<Code code="brew install haloydev/tap/haloy" lang="bash" />


### JavaScript Package Managers

<CodeTabs defaultValue="npm">
  <Fragment slot="tabs">
    <CodeTabTrigger value="npm">npm</CodeTabTrigger>
    <CodeTabTrigger value="pnpm">pnpm</CodeTabTrigger>
    <CodeTabTrigger value="bun">bun</CodeTabTrigger>
  </Fragment>

  <CodeTabPanel value="npm">
    <Code code="npm install -g haloy" lang="bash" />
  </CodeTabPanel>

  <CodeTabPanel value="pnpm">
    <Code code="pnpm add -g haloy" lang="bash" />
  </CodeTabPanel>

  <CodeTabPanel value="bun">
    <Code code="bun add -g haloy" lang="bash" />
  </CodeTabPanel>
</CodeTabs>


#### Project Dev Dependency

For project-specific installations, add haloy as a dev dependency:

<CodeTabs defaultValue="npm">
  <Fragment slot="tabs">
    <CodeTabTrigger value="npm">npm</CodeTabTrigger>
    <CodeTabTrigger value="pnpm">pnpm</CodeTabTrigger>
    <CodeTabTrigger value="bun">bun</CodeTabTrigger>
  </Fragment>

  <CodeTabPanel value="npm">
    <Code code="npm install --save-dev haloy" lang="bash" />
  </CodeTabPanel>

  <CodeTabPanel value="pnpm">
    <Code code="pnpm add -D haloy" lang="bash" />
  </CodeTabPanel>

  <CodeTabPanel value="bun">
    <Code code="bun add -D haloy" lang="bash" />
  </CodeTabPanel>
</CodeTabs>

Then use it via npm scripts in your `package.json`:

<Code code={`
  // ...
  {
    "scripts": {
      "deploy": "haloy deploy",
      "deploy:staging": "haloy deploy --target staging",
      "deploy:prod": "haloy deploy --target production",
      "status": "haloy status"
    }
  }
`} lang="json" />


## Verify Installation

Check that `haloy` is installed correctly:

<Code code="haloy version" lang="bash" />

## Uninstalling

See the [Uninstalling guide](/docs/uninstalling) for instructions on removing Haloy from your system.

## Next Steps

- [Install Haloy on your server](/docs/server-installation)
- View [haloy CLI Command Reference](/docs/haloy)
- [Set up shell completion](/docs/shell-completion)
